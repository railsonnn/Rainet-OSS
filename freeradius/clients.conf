# FreeRADIUS Clients Configuration
# Define NAS (Network Access Server) clients that can connect to RADIUS

# MikroTik Router
client mikrotik {
    ipaddr = 0.0.0.0/0
    secret = testing123
    shortname = mikrotik-router
    nastype = mikrotik
}

# Localhost for testing
client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    shortname = localhost
}
